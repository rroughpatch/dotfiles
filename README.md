# dotfiles

My macOS shell configuration.

## Install

```sh
./install.sh
```

The installer backs up existing files to `~/.dotfiles-backup/` before linking.

## Files

- `zshrc` -> `~/.zshrc`
- `zprofile` -> `~/.zprofile`
